// PDPA privacy-policy version the user consents to at sign-in (US-007). Bump on
// any change to the data-use / notification consent copy so re-consent is tracked.
export const CONSENT_VERSION = '1.0';
