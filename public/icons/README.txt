PWA icons (referenced by /manifest.webmanifest + the service-worker push handler):

  icon-192.png        192x192  (any)
  icon-512.png        512x512  (any)
  maskable-512.png    512x512  (maskable, safe-zone padded)

Current files are brand-orange PLACEHOLDERS shipped by Engineer #19 (Platform/PWA):
a #F5811F field with a white "cloche" mark, generated dependency-free so the app
is installable and passes Lighthouse today. Plan 05 (brand/icon kit) replaces them
with the final brand art by dropping new PNGs at the SAME paths + sizes — no manifest
or layout change required. New brand mark: navy ink + orange cloche-as-"oo" wordmark.
