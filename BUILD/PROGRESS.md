# Build Progress

Each scheduled task ticks its box here and appends a short note (its own commit).
Status: ☐ not started · ◐ in progress · ✅ done · ⚠ blocked (see note).

## LOCAL RUN — bring a fresh clone up (one page)
Prereqs: Node ≥ 18.18, npm, and the Supabase CLI (`npm i -g supabase`) for db tasks.
1. **Install:** `npm install`
2. **Env:** `cp .env.example .env.local` and fill Supabase URL + keys (and optionally WhatsApp/VAPID).
   The app boots without Supabase env for UI work — auth/db calls just no-op/redirect.
   Keys needed: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`.
3. **DB schema:** either `npm run db:migrate` (applies `supabase/migrations/000{1,2,3}_*.sql` via
   `supabase db push`) **or** paste `supabase/setup.sql` into the Supabase SQL editor (combined schema+seed).
4. **Seed:** `npm run db:seed` (idempotent — zones, sample menu, today's closed session).
5. **Dev:** `npm run dev` → http://localhost:3000  (Customer `/`, Menu `/menu`, Rider `/rider`, Login `/login`).
6. **Verify green:** `npm run typecheck` · `npm run lint` · `npm run test:unit`.
Auth setup (free, no paid SMS): see `BUILD/AUTH_SETUP.md` — Email OTP works locally now; WhatsApp OTP is deploy-gated.

## Baseline green (task 1-1) — verified 2026-06-22
- [x] `npm install` · `npm run typecheck` · `npm run lint` · `npm run test:unit` all green.
- [x] Auth loads: `api/auth/otp/{request,verify}` import cleanly; verify writes `consent_at` (US-007)
      and returns `{ role, lang }`; request maps rate errors → HTTP 429 (US-006).
- [x] Middleware guards `/operator` `/rider` `/checkout` `/history`; unauth → `/login?next=`,
      wrong-role → `/`; `requireRole`/`homeForRole`/`RoleError` exported by `src/lib/auth/roles.ts` (US-003/004).
- [x] Single active session in DB: `daily_session.session_date … unique` + partial unique index
      `one_open_session … where status='open'` in `0001_init.sql` (US-030 / FR-S-06).
- [x] i18n loads: `getI18n()`/`getLocale()` read `NEXT_LOCALE` cookie; `messages/en.json` + `ar.json`
      parse and AR mirrors EN exactly (58 keys, asserted in the smoke test).
- [x] Dev seed runs idempotently (`supabase/seed.sql`); `.env.example` covers every `process.env` key used in `src`.
- Smoke test: `tests/unit/foundation.test.ts` (10 assertions — i18n config/dictionaries, AR↔EN mirror, role helpers).

## Foundation
- ✅ Seed scaffold committed one-file-per-commit (77 commits) and pushed to `main`.
- ✅ `npm install` + `npm run typecheck` green (fixed Supabase cookie typings).

## Day 1 — 2026-06-22
- ✅ 1-1 Foundation audit & green baseline
- ✅ 1-2 Design tokens + shared UI + 4 states + RTL
- ✅ 1-3 App shell & role layouts + bottom nav
- ✅ 1-4 i18n completeness + language switcher
- ✅ 1-5 Customer Home / Live Status — built early (interactive session); verify & polish only, do NOT rebuild

## Day 2 — 2026-06-23
- ✅ 2-1 Menu list + item detail — built early; verified green + added the missing DoD test (do NOT rebuild)
- ✅ 2-2 Cart
- ✅ 2-3 Checkout: fulfilment + zone + address
- ✅ 2-4 Checkout: payment + gate + place order
- ✅ 2-5 Confirmation + tracking + history

## Day 3 — 2026-06-24
- ✅ 3-1 Dashboard + one-tap Open/Close/Sold-Out
- ✅ 3-2 Daily setup (qty/cutoff/window/zones)
- ✅ 3-3 Menu manager
- ✅ 3-4 Live order board
- ✅ 3-5 Order detail / verify / refuse / broadcast / EOD

## Day 4 — 2026-06-25
- ✅ 4-1 Notifications end-to-end
- ✅ 4-2 Realtime correctness
- ✅ 4-3 Rider flow completion & polish
- ✅ 4-4 Automation & inventory integrity
- ✅ 4-5 PWA shell / offline / install / push

## Day 5 — 2026-06-26
- ✅ 5-1 Unit tests (Vitest)
- ✅ 5-2 E2E tests (Playwright)
- ✅ 5-3 Security + a11y + PDPA hardening
- ✅ 5-4 Deploy-prep (env/CI/config/README)
- ☐ 5-5 Final integration + handover + release tag

---
## Notes log
- (foundation) Base green on Node; Next 14.2.15 has a flagged security advisory — bump to patched 14.2.x in task 5-3.
- (21 Jun · interactive session) Built early & pushed — verify/polish only, do NOT rebuild: middleware env-guard (app boots without Supabase env); login phone normalize (strips spaces/dashes); Customer Home `/` (SCR-C-01) + `customer/status-hero`; Menu `/menu` + `/menu/[id]` (SCR-C-02/03) + `customer/menu-card`. Added `supabase/setup.sql` (combined schema+seed). `.env.local` exists locally with real Supabase (new `sb_publishable`/`sb_secret` keys mapped onto NEXT_PUBLIC_SUPABASE_* / SUPABASE_SERVICE_ROLE_KEY — keep this mapping; do NOT switch to @supabase/server). Pending owner action: run setup.sql in Supabase SQL editor; configure a phone provider/test number for OTP.
- (21 Jun · interactive session) AUTH = two free channels, no paid SMS provider — keep this design: (a) **Email OTP** (`/api/auth/email/{request,verify}`, Supabase built-in, works now); (b) **WhatsApp OTP** via Supabase **Send-SMS hook** → `/api/auth/sms-hook` delivers the code over WhatsApp (phone `signInWithOtp`/`verifyOtp` flow UNCHANGED; Supabase owns the session — do NOT switch to a custom session/JWT). Login page has a Phone/Email toggle. Full steps in `BUILD/AUTH_SETUP.md`. WhatsApp is deploy-gated (Supabase can't reach localhost).
- (22 Jun · task 1-2) Design system locked: audited tokens vs Fig.16-2 — added the D-15 **type scale** (`text-h1/h2/title/body/caption`) and the **teal #2E8B7B** status accent to `tailwind.config.ts`; added `.btn-ghost` to `globals.css` (Primary/Secondary/Ghost/Rider buttons now complete — Rider keeps `.btn-open` green). Added **Skeleton** loader to `states.tsx` (Fig.16-4 shows skeleton ≠ spinner). Fixed **Stepper** tap targets 36→44px (`min-h-tap min-w-tap`). RTL audit: no hard left/right in `src/components/ui/*` (all flex/logical), `html[dir='rtl']` font swap intact — primitives mirror via auto-flip (US-012). New unit test `tests/unit/ui-primitives.test.ts` (11 tests) renders every primitive in en+ar and asserts 44px tap classes; needed `esbuild:{jsx:'automatic'}` in `vitest.config.ts` so JSX components render (they use Next's runtime, no `import React`). typecheck/lint/test:unit all green (21 tests). Follow-ups (out of scope here, for the screens that need them): Segmented control, Toggle, Radio, Zone chip primitives (shown in Fig.16-1, used by checkout/payment 2-3/2-4) — build inline there or extract then. Notes: `cream` = Fig's "Zebra" #F4F1EE (same hex, kept name); radius 10/14 from Fig left out (only card 12 / control 8 in use).
- (22 Jun · task 1-4) i18n completeness + language switcher (US-012). Audit result: `messages/en.json` and `ar.json` are already a complete key-for-key mirror (58 keys, AR translated); every dictionary key referenced in code (`getI18n()`/`t.*` on the Rider pages + `delivery-card.tsx`) resolves — no missing keys to add. NOTE: the customer Home/Menu/Status screens, login, and offline page use an inline `lang==='ar' ? … : …` bilingual pattern (from the early interactive build) instead of the dictionary — they ARE bilingual, just via a second mechanism; migrating them onto `messages/*` is a later polish (out of 1-4 scope, "do not rebuild"). Shipped `src/components/ui/lang-switch.tsx`: `setLocaleCookie()` is now the SINGLE NEXT_LOCALE cookie writer (path '/', max-age 1y) and `LangToggle` in controls.tsx was refactored to call it — no divergent cookie mechanism. `LangSwitch` is the labelled header variant (≥44px tap target, current→target labels, RTL-aware, stable English aria-label); `LangToggle` stays the compact chip. Persistence path verified: layout.tsx + i18n/server.ts `getLocale()` both read the same cookie; `dir()`/`isLocale()` from config.ts in use. Tests: `tests/unit/i18n-parity.test.ts` (parity guard — identical key sets, non-empty, AR≠EN except `duitnow`, {{var}} token parity — plus LangSwitch render/tap-target) and `tests/e2e/i18n-switch.spec.ts` (switch→AR, reload, assert `<html dir=rtl lang=ar>` persists + cookie). typecheck/lint/test:unit all green (29 unit tests). FOLLOW-UP: task 1-3 (app shell) is not done yet — no public page mounts a switcher (LangToggle is only on the auth-gated `/rider`), so 1-3 should mount `LangSwitch` in the customer/operator headers; the e2e auto-drives the on-screen switcher once present (it falls back to the cookie until then).
- (22 Jun · task 1-3) App shell & role layouts + bottom nav. Hoisted the shell out of pages into route-group layouts so every customer/operator screen drops into a consistent frame: `src/app/(customer)/layout.tsx` (public, no auth) and `src/app/(operator)/layout.tsx` (defence-in-depth `requireRole('operator')` → on `RoleError` redirect unauthorized→`/login`, forbidden→`homeForRole('customer')`=`/`, behind `middleware.ts`). Each layout = `OfflineBanner` (top) + the standard `mx-auto min-h-dvh max-w-md space-y-4 p-4 pb-24` `main` frame (mirrors the rider page) + per-role `BottomNav`. Nav config lives in `src/lib/nav/items.ts` (`customerNav`/`operatorNav`, labels from the dictionary): customer = Home `/` · Menu `/menu` · Orders `/orders` · Account/History `/history`; operator = Board `/operator/board` · Setup `/operator/setup` · Menu `/operator/menu` · End of day `/operator/end-of-day` (per Fig.16-1). Added group `loading.tsx` (spinner only — the layout owns `main`, so no nested `<main>`). Since the layout now owns the frame, unwrapped the early-built customer pages' own `<main>` (home/menu/menu[id]) to fragments — content/components untouched (verify & polish only). DECISIONS: (a) added two genuinely-missing dictionary keys `home`/`orders` to en+ar (no `home`/`orders` key existed; §3 forbids hard-coded labels) — kept item-4 on the existing `order_history` key per brief; (b) mounted 1-4's public `LangSwitch` in both shell headers (1-4's note explicitly handed this to 1-3 — it's the only language control on public pages; the i18n e2e auto-drives it). Test: `tests/unit/shell-nav.test.ts` (9) — per-role nav hrefs/labels bilingual, role-appropriate (disjoint, operator routes scoped), each group renders its own nav + switcher, and the operator guard redirects a customer→`/` and an unauth visitor→`/login` (US-002/003/004). typecheck/lint/test:unit all green (38 unit tests). Note: built on top of 1-4's 5 local-but-unpushed commits (concurrent run) — they are complete & green, so this push carries both. FOLLOW-UP: operator pages (`/operator/board` etc.) arrive in day 3; Playwright e2e for the redirect is scripted as a unit test here (Playwright runs on test days).
- (23 Jun · task 2-1) Menu list + item detail were already built & pushed in the early interactive session (`menu/page.tsx`, `menu/[id]/page.tsx`, `customer/menu-card.tsx`) — brief says verify & polish only, do NOT rebuild. Verified against the DoD: available items show photo (bg-image fallback 🌯) + bilingual name + `formatMYR` price and link to `/menu/[id]`; unavailable items are dimmed (`opacity-60`) + carry an Unavailable/غير متوفر badge and are NOT wrapped in a link (can't be tapped to add); 4 UI states present (Empty/Error on the list, loading via the group `loading.tsx`, `OfflineBanner` in the customer layout); ≥44px tap targets on the card. Detail CTA routes to `/login` as a placeholder until task-2-2 wires the cart. The only DoD gap was the required render test, so added `tests/unit/menu-card.test.ts` (6 tests, available vs unavailable in en+ar, price/link/dim/badge assertions). NOTE: menu screens are bilingual via the early-build inline `lang==='ar' ? … : …` pattern (not `messages/*`) — same as Home/Status; migrating them onto the dictionary stays a later polish (per the 1-4 note), so no new `messages/*` keys were needed. typecheck + lint clean; test:unit green (44 unit tests).
- (23 Jun · task 2-5) Confirmation + live tracking + history (US-018 / US-021 / US-020). Created `src/app/(customer)/orders/[id]/page.tsx` — a Server Component that fetches `getOrder(id)` + resolves bilingual line names via `listMenu` (one menu read, no per-line N+1), renders the confirmation header (order_no, fulfilment type, ✓ order placed) and hydrates `customer/order-tracker.tsx`, a `useOrderStatus(id, initial)` client island that re-renders the **reused** `Timeline` from the live `order.status` so an operator/rider advance reflects in <2s without refresh (NFR-P-01, aria-live banner), with order lines + `formatMYR` line/total + payment method·status. Cancelled → the Timeline's cancelled branch + `cancel_reason`. Created `history/page.tsx` — `listMyOrders()` rows (order_no, MYT date via new `formatMyt` in `utils/time.ts`, item count via `items_count`, `formatMYR` total, `OrderStatusChip`) each linking to `/orders/[id]`; all 4 states (Empty `no_orders`, Error, loading via group `loading.tsx`, OfflineBanner from the layout), ≥44px rows, RTL-safe (flex/logical only). Added dictionary keys to en+ar (order_no, items, items_count, order_total, payment_status, pay_pending/submitted/verified/rejected, no_orders, no_orders_hint, final_status, order_not_found). Test `tests/unit/order-tracking.test.ts` (6) — timeline highlight at a status, live re-render advances one step, cancelled branch, tracker render (lines/total/payment), `formatMyt`. typecheck + lint clean; test:unit green (59 tests). CONCURRENCY: the cart (2-2) and checkout (2-4) runs were writing `messages/*.json` simultaneously — to keep the EN/AR key-parity test green I reconciled both catalogs to the union, adding AR translations for the 18 checkout keys the 2-4 run had only put in EN (their later commits can override those values; last-write-wins is still parity-safe). FOLLOW-UPS (out of scope here): the `/orders` customer-nav tab still has no index page (only `/orders/[id]` exists) — add an orders index or repoint the nav later; the tracking screen's rider card (wireframe C-06) waits on day-4 rider assignment.
- (23 Jun · task 2-2) Cart (US-013 / FR-C-05). Created `src/lib/cart/store.ts` — a dependency-free client cart store (Zustand isn't a dep, per brief): a pure, unit-tested `cartReducer` + `itemCount`/`total` selectors (total via the money util's `sumLines`), bound to React with a tiny `useSyncExternalStore` singleton (no provider needed → works across route groups incl. the future auth-gated `/checkout`), persisted to `localStorage` (`fahman.cart.v1`) with SSR-safe post-mount hydration and a corrupt-storage error path. Line shape `{menu_item_id,name_en,name_ar,unit_price,qty}` is a frozen price snapshot. Created `customer/cart-line.tsx` (editable row: bilingual name, unit price, `Stepper` qty, Remove, live `subtotal`; ≥44px) and `customer/add-to-cart.tsx` — the client control that wires the menu item-detail Add into `add()` (Add button ↔ stepper + View cart; decrement-to-0 removes). `src/app/(customer)/cart/page.tsx` lists lines with live count (`cart_count`) + `formatMYR(total)` and a sticky `/checkout` CTA, with all 4 states (loading=hydration, error=corrupt cart, empty, `OfflineBanner`). Wired `menu/[id]/page.tsx` (now `getI18n`) to mount `<AddToCart>` instead of the sign-in placeholder. Added i18n keys remove/subtotal/cart_total/cart_count/view_cart/browse_menu (en+ar). Test `tests/unit/cart-store.test.ts` (9) — add/inc/dec(removes at 0)/remove/clear/qty-cap/immutability + count/total selectors. typecheck + lint clean; test:unit green (59 tests). DECISIONS: (a) used a `useSyncExternalStore` store instead of the brief's suggested context/provider — no new dep, no layout edit, and the cart stays global across route groups (cleaner for the 2-4 `createOrder` read); (b) the menu LIST cards still navigate to detail to add (each card is a single tap-target link — an inline +/- would nest a button inside the anchor); add a compact list Add later if wanted. CONCURRENCY: my 6 cart keys reached `messages/*.json` via a sibling's shared-tree commit (the working tree is shared across the day-2 runs); verified committed HEAD en/ar stay mirrored 97/97. FOLLOW-UP: `/cart` has no bottom-nav entry yet (customerNav = Home/Menu/Orders/History) — reach it via the detail View-cart link for now; add a cart tab/badge in a later polish.
- (22 Jun · task 1-1) Foundation audited green: typecheck/lint/test:unit all pass. Fixed 2 `eqeqeq` lint errors (`!= null` → `!== null` on `number|null` params in `rider/delivery-card.tsx` + `lib/domain/rider.ts` — semantics unchanged). Added `tests/unit/foundation.test.ts` (10 assertions) so `vitest run` has a file and is green; it locks the AR↔EN mirror (58 keys) + role-home routing. Verified auth/middleware/RLS-intent/daily-session invariant/i18n/seed/env per the brief (see Baseline checklist + LOCAL RUN above). Follow-ups for later tasks: (5-3) bump Next off 14.2.15 advisory; (5-1/5-2) widen unit + add Playwright e2e — this smoke test is intentionally import-safe (mocks `server-only`/`next/headers`), not a full auth/db test; owner still needs to run `setup.sql` + configure an OTP channel before live auth works.
- (23 Jun · task 2-3) Checkout part 1 — fulfilment type + zone + address (US-014 / FR-C-06, US-015 / FR-C-07). Created `src/app/(customer)/checkout/page.tsx` — a Server Component that fetches active zones (`listZones(true)`) + the customer's saved addresses (`listAddresses()`, best-effort) and hands them to the client island; placing the order stays in 2-4. Created `customer/checkout-flow.tsx` (the step-1 client island — holds the draft `{type, zone_id, address_id}`, a Delivery/Walk-in-Pickup segmented toggle, and a gated "Continue to payment"), `customer/zone-picker.tsx` (active-zone radio rows + empty/closed state), and `customer/address-form.tsx` (saved-address one-tap reuse + new-address entry validated by `addressSchema` and saved via the `createAddress` server action, with an optional map pin from manual lat/lng or `navigator.geolocation` — no paid map API). Delivery-only fields render only for Delivery; the gate (added `deliveryReady` to `lib/utils/schemas.ts`, mirroring `createOrderSchema.refine`) blocks Continue until an active zone AND an address are chosen (clear bilingual prompt), pickup needs neither. All 4 states: empty/closed (no active zones), loading (group `loading.tsx`), error (zones fetch fails → `ErrorState`), offline (`OfflineBanner` in the layout); ≥44px taps; RTL via logical `text-start`/flex. Test `tests/unit/checkout-gate.test.ts` (5) — `deliveryReady` pickup-vs-delivery + zone+address requirement, and `ZonePicker` (lists active zones, aria-checked selection, bilingual empty state). typecheck + lint clean; full test:unit green (64 tests). DECISIONS: (a) bilingual via the established inline `lang==='ar' ? … : …` customer-screen pattern (same as Home/Menu/Status/cart per the 1-4 note) rather than new `messages/*` keys — `messages/en.json`/`ar.json` were being written live by the concurrent 2-2/2-4/2-5 runs in this shared tree, so touching them risked clobbering siblings or committing a broken-parity state; the brief's named keys can join the dictionary in the same later i18n-consolidation polish as the other customer screens. (b) Added a 4th component (`checkout-flow.tsx`) beyond the 3 the brief named because `page.tsx` must stay a Server Component yet the step needs shared client state — it IS the "client island" the brief calls for. HANDOFF to 2-4: the validated draft is written to `sessionStorage["fahman.checkout.draft"]` and Continue navigates to `/checkout/payment` (does not exist yet — 2-4 should create that route, or refactor the flow to render the payment region inline on `/checkout`, reading the draft). The `/cart` back-link depends on 2-2's cart page (now committed).
- (23 Jun · task 2-4) Checkout part 2 — payment method + ordering gate + place order (US-016/017, FR-C-08/09/10). Created `customer/proof-upload.tsx` (DuitNow QR proof: image-only ≤5 MB client validation → upload to the `payment-proofs` Supabase Storage bucket via the browser anon client → returns the public URL as `proof_url`; bg-image preview, Remove, ≥44px) and `customer/payment-section.tsx` — exports `PaymentSection` (prop-driven, unit-testable core: COD vs DuitNow QR radio + the gate + place-order) and `CheckoutPayment` (page wrapper that sources cart lines via `useCart()` + the 2-3 `sessionStorage["fahman.checkout.draft"]`, clears the cart and routes to `/orders/[id]` on success). Created `src/app/(customer)/checkout/payment/page.tsx` — the `/checkout/payment` route 2-3 handed off, a Server Component seeding live status + `cutoff_time` from `getStatus()`. Place-order POSTs `{type,zone_id,address_id,payment_method,proof_url,items}` to `/api/orders` with a fresh `Idempotency-Key: crypto.randomUUID()` header (the route injects it into the race-safe `createOrder` → `place_order` RPC — never decremented client-side); maps every createOrder error code to a bilingual message. Gate (one clear reason; Place disabled): offline / shop Closed (`shop_closed_msg`) / Sold-Out or 0 remaining (`sold_out_msg`) / past cut-off (`err_past_cutoff`) / delivery missing zone+address (`err_delivery_requires_address`); DuitNow blocks until a proof is attached, COD needs none. 4 states: loading (cart hydration), empty (`EmptyState`), error (`ErrorState` on rejected placement), offline (`OfflineBanner` + inline reason). Added 18 i18n keys to en+ar (payment_method, cod_note, duitnow_note, scan_to_pay, proof_required/attached, upload_proof_hint, change_image, proof_invalid_type/too_large/upload_failed, placing_order, err_shop_not_open/past_cutoff/sold_out/delivery_requires_address/item_unavailable/order_generic), reusing the existing `remove` key; en/ar mirrored 97/97. Test `tests/unit/payment-section.test.ts` (8): COD-no-proof places, DuitNow-requires-proof, blocked-when-Closed, Sold-Out (status + 0-qty), past-cutoff, delivery-needs-address, empty-cart, bilingual. typecheck + lint clean; test:unit green (72 tests). DECISIONS: (a) created the `/checkout/payment` page beyond the 2 named files because 2-3 already navigates there — without it the flow 404s; (b) split `PaymentSection` (pure/testable) from `CheckoutPayment` (live cart/draft wiring) so the gate is unit-tested without the global store. OWNER SETUP (no secrets committed): create a public Supabase Storage bucket `payment-proofs` with an authenticated-insert policy, and drop the merchant QR at `public/duitnow-qr.png` (a missing file just shows the "Scan to pay" caption). CONCURRENCY: a parallel run had already committed the EN+AR payment keys into this shared tree (2-5 reconciled AR) — verified my components consume those exact keys and parity stays 97/97.
- (23 Jun · QA sweep — end of Day 2) Autonomous QA & consistency pass over the customer ordering flow.
  **GREEN:** `npm run typecheck` · `npm run lint` (no warnings) · `npm run test:unit` (9 files / 72 tests) all pass, verified across 3 consecutive runs. Day-2 flow is complete end-to-end: Home → Menu → item detail → Add-to-cart → Cart → Checkout (fulfilment/zone/address) → Payment (COD/DuitNow proof + ordering gate) → place via race-safe `place_order` RPC w/ Idempotency-Key → /orders/[id] confirmation+live tracking → /history. Bilingual messages at 97/97 parity (no missing/empty keys); 4 UI states present (layout owns OfflineBanner; group `loading.tsx`; empty/error in pages); tap targets ≥44px (`min-h-tap` on btn/chip/field/nav/stepper); RTL via logical props (arrow glyphs mirror, e.g. `← Menu` / `→ المنيو`); inventory never decremented client-side.
  **FIXED (one file per commit):** (1) `src/app/(customer)/orders/page.tsx` — the customer bottom-nav **"Orders" tab pointed at `/orders` which had no index → 404**; added an index that redirects to the existing order list `/history` (order detail stays at `/orders/[id]`). (2) `src/components/ui/states.tsx` — `ErrorState`'s "Try again" button was **hard-coded English** (AR users saw English on every retry); now localised AR/EN from `<html lang>` post-mount, with an optional `retryLabel` override. Both commits authored as Alsakkaf-dev.
  **MISSING (for a later run — larger gaps, not done here):** (a) consolidate the canonical order-list route under `/orders` (currently the list lives at `/history` and is reached by BOTH the "Orders" and "Account/History" nav tabs — redundant) and build a real **Account** screen for the 4th tab (Fig.16-1 shows Home·Menu·Orders·Account); (b) `/cart` still has no bottom-nav entry — reachable only via the item-detail "View cart" link (carried over from the 2-2 note); (c) optional polish: migrate the inline `lang==='ar'?…:…` customer-screen strings onto `messages/*` (known 1-4 follow-up), and localise the Stepper's `decrease`/`increase` aria-labels.
  **CONCURRENCY:** a Day-3 operator build is running in this shared tree right now — left untouched: modified `messages/{en,ar}.json` and untracked `src/app/(operator)/operator/**`, `src/components/operator/**`, `tests/unit/operator-controls.test.ts`. Committed only my own paths; the earlier transient test blip was that sibling mid-writing `messages/*` during the parity test (re-ran clean).
- (24 Jun · task 3-1) Operator dashboard `/operator` + one-tap Open/Close/Sold-Out (US-023/024/025, FR-O-01/02/03). Created `src/app/(operator)/operator/page.tsx` (Server Component, `force-dynamic`) — fetches the day snapshot via `getStatus()` + the orders count via `boardList()` (degrades to 0 if the role read fails), renders the title header, an orders-count stat card (with a quick link to `/operator/board`; live board lands in 3-4), a needs-setup notice when `qty_total<=0` (the **empty state** — `openShop` guards qty>0, so this guides the operator to `/operator/setup`), and mounts the controls island. Created `src/components/operator/state-controls.tsx` (`'use client'`) — live `StatusBadge`+`QtyCounter` via `useShopStatus(initial)` (shop:status channel → propagates to all clients <2s, NFR-P-01) and three `h-16` (64px) buttons: **optimistic** (tapped status shows at once, then the server action runs; on `!ok` it rolls back to the live status and shows an inline `role="alert"` error; on ok it `router.refresh()`es), with the current-status button disabled. The three server actions (`openShop`/`closeShop`/`setSoldOut`) are passed in as props from the page so the island carries no server-only imports and is directly unit-testable. Test `tests/unit/operator-controls.test.ts` (6) — the pure `ACTION_RESULT` transition map (open→open, close→closed, sold_out→sold_out), all 3 large buttons render, live badge/qty from the seed, only the current-status button is disabled, qty hidden before setup, and bilingual AR. DECISIONS: (a) the `(operator)/layout.tsx` (built in 1-3) already owns the shell — `<main>`, `OfflineBanner`, the public `LangSwitch` header, and the `BottomNav` (`operatorNav`: Board/Setup/Menu/EOD) — so, unlike the brief (written before that layout), this page renders its content as a **fragment into that frame** and does NOT re-add a LangToggle/BottomNav/OfflineBanner (mirrors how 1-3 unwrapped the customer pages); (b) **no `messages/*` edits** — reused existing keys (`operator_dashboard`/`open_shop`/`close_shop`/`set_sold_out`/`orders`/`quantity`/`order_board`/`daily_setup`/`error_generic`); the only non-dict strings are the "Shop status"/needs-setup helper lines, kept as the inline `lang==='ar'?…:…` pattern already used by `status-hero.tsx` — this avoided touching `messages/*` while task-3-2 was mid-writing it in the shared tree. typecheck + lint clean; full test:unit green (10 files / 78 tests). FOLLOW-UPS (out of scope): the layout's 4-item `BottomNav` (CMP-U-14 cap) has no Dashboard/Broadcast entry the brief mentioned — add once the broadcast route (3-5) exists and the cap is revisited; specific openShop failure reasons (outside 1–7 PM trading hours / qty 0) currently surface the generic error (the needs-setup notice pre-empts the qty-0 case) — map them to dedicated bilingual keys in a later i18n pass.
- (24 Jun · task 3-2) Daily setup `/operator/setup` (US-026..029, FR-O-04..07). Created `src/app/(operator)/operator/setup/page.tsx` (Server Component, `force-dynamic`) — pre-loads current values via `getStatus()` and **all** zones via `listZones()` (no `activeOnly`, so inactive zones show for toggling), renders the title header + localized MYT date line, and hands data to the form (or `ErrorState` if zones fail to load). Created `src/components/operator/setup-form.tsx` (`'use client'`) — one form persisting today's total quantity, cut-off time (`<input type=time>`→`HH:mm`), delivery window (text) and active delivery-zone toggles via `configureSession`, which sets **qty_remaining=qty_total** (US-026) and activates exactly the selected zones, deactivating the rest (US-029). Client-side validation mirrors `configureSessionSchema` directly (no duplicated rules); Save is gated until valid + online. 4 UI states: success toast (`role=status`), inline error alert (`role=alert`), empty (no-zones `EmptyState`), offline (gates Save + hint; layout owns the top banner). The `configureSession` server action is passed in as a **prop** from the page so the island has no server-only imports and is unit-testable. Added 12 i18n keys to en+ar (today/save_setup/saving/setup_saved/setup_error/setup_invalid/setup_autoclose_hint/no_zones/no_zones_hint/delivery_window_placeholder/zone_active/zone_inactive); reused existing `quantity`/`cutoff`/`delivery_window`/`zones`/`daily_setup`/`offline`/`error_generic`. Test `tests/unit/operator-setup.test.ts` (8) — `configureSession` sets qty_remaining=qty_total + cutoff/window + zone deactivate-all-then-activate-selected (and skips re-activate when none) + rejects invalid input; SetupForm renders the 4 labels/Save, zone toggles with `aria-checked`, ≥44px taps, the no-zones empty state, and bilingual AR. typecheck + lint clean; full test:unit green (11 files / 86 tests). DECISIONS: (a) quantity uses a numeric `<input type=number>` (0–1000) flanked by −/+ buttons rather than the `Stepper` primitive — the brief allows "Stepper **or** numeric input", and typing beats tapping to ~40–60 portions (kept the same `btn-secondary min-h-tap` ± glyphs + `decrease`/`increase` aria-labels for primitive consistency); (b) button labeled **Save setup** (not the wireframe's "Save & open shop") — opening the shop is 3-1's dashboard action and `configureSession` does not open (and `openShop` is trading-hours-gated), so this stays in-scope to persisting setup. FOLLOW-UPS (out of scope): zone names are single-field (`Zone.name`, treated as a proper noun shown in both locales, like `duitnow`) — add bilingual zone names if ever needed; a "Save & open shop" combined CTA could be offered on the dashboard once 3-1/3-2 integrate.
- (22 Jun · task 3-3) Menu manager `/operator/menu` (US-031, FR-O-08). Created `src/app/(operator)/operator/menu/page.tsx` (Server Component, `force-dynamic`) — loads items via `listMenu()` (already sorted by `sort_order`) and hands them + the operator-only server actions to the editor; `(operator)/layout.tsx` owns the shell so this renders a fragment. Created `src/components/operator/menu-editor.tsx` (`'use client'`) — an add/edit form (bilingual name + description, MYR price, sort order) that mirrors `menuUpsertSchema` client-side (`draftToInput` maps the draft → upsert shape; `id` present ⇒ update, absent ⇒ insert) and submits to `upsertMenuItem`, plus a list of existing items showing photo thumb (🌯 fallback), bilingual name, `formatMYR` price and a `role=switch`/`aria-checked` availability toggle wired to `setAvailability`. After every save `router.refresh()` re-renders the server snapshot so the customer menu reflects the change within seconds (Scenario A). Hidden items dim + carry `data-available=no` (Scenario B; the customer menu-card already blocks adding unavailable items). Photo upload reuses the proof-upload pattern: client image/≤5 MB guard → browser anon client `storage.from('menu-photos').upload(...)` → public URL saved as `photo_url`. 4 states: empty (`EmptyState` no-items), loading (upload busy label), error (inline `role=alert` on save fail), offline (layout banner). Added 24 i18n keys to en+ar (new_item/add_item/edit_item/name_en/name_ar/description_en/description_ar/price_myr/sort_order/photo/available/unavailable/save_item/item_saved/item_error/item_form_invalid/no_items/no_items_hint/upload_photo/change_photo/photo_hint/mark_available/mark_unavailable); reused the generic image-error keys (proof_invalid_type/too_large/upload_failed). Test `tests/unit/menu-editor.test.ts` (11) — domain upsert insert-vs-update + requireRole('operator') + validation_error, `setAvailability` writes `available=false` for the id, `draftToInput` mapping, and MenuEditor render (list + aria-checked toggle, form labels, ≥44px taps, empty state, bilingual AR). typecheck + lint clean; full test:unit green (12 files / 97 tests). OWNER SETUP (no secrets committed): create a public Supabase Storage bucket `menu-photos` with an authenticated-insert (operator) policy — a missing bucket just fails the upload with the inline error; names/price still save. FOLLOW-UPS (out of scope): item delete/reorder UI (only hide via availability + manual sort_order today); migrate the menu screens off the inline AR pattern in the later i18n consolidation.
- (24 Jun · task 3-4) Live order board `/operator/board` (US-032/033/034, FR-O-09/10/11). Created `src/app/(operator)/operator/board/page.tsx` (Server Component, `force-dynamic`) — seeds today's orders via `boardList()` and hands them + `advanceOrder`/`dispatchOrder` to the client board. Created `src/components/operator/order-chip.tsx` (`'use client'`) — one order chip (order_no, type, `items_count`, `formatMYR` total, `OrderStatusChip`) with an Advance button whose target is computed from `ORDER_TRANSITIONS` (never hard-coded) via the exported pure helpers `forwardStatus(order)` + `canDispatch(order)`; a Ready **delivery** order shows Dispatch (→ out_for_delivery), a Ready **pickup** order advances to delivered. The action re-validates server-side and an illegal move returns `invalid_transition` (inline `role=alert`); on success the realtime UPDATE moves the chip, no refresh. Created `src/components/operator/board-column.tsx` — one status column (localized `OrderStatusChip` header + live count badge + chips). Created `src/components/operator/order-board.tsx` (`'use client'`) — the board wrapper using `useOrderBoard(initial)`; INSERT/UPDATE stream live so a placed order lands in New within ~2s (US-032), grouped into the 6 lifecycle columns (cancelled drops off). 4 states: empty (`EmptyState` no-orders), loading (group `loading.tsx`), error (`ErrorState` on board fetch fail), offline (layout banner). Added 3 i18n keys to en+ar (advance/board_hint/err_invalid_transition); reused order_board/no_orders/delivery/pickup/dispatch/items_count/error_generic. Tests: `tests/unit/order-board.test.ts` (9) — forwardStatus/canDispatch per stage, `advanceOrder` legal step (updates + fires exactly one notification) vs illegal (`invalid_transition`, no write, no notification), and OrderBoard render (column grouping, Dispatch-vs-Advance, empty state, ≥44px, bilingual AR); `tests/e2e/order-board.spec.ts` — live-insertion spec (places an order via the API, polls the New column), self-skips without an operator session/Supabase (runs on a seeded stack in 5-2). typecheck + lint clean; full test:unit green (13 files / 106 tests). DECISION: added a 4th file `order-board.tsx` beyond the brief's 3 — the page is a Server Component but the board needs the `useOrderBoard` client hook, so this is the "client wrapper" the brief calls for (same pattern as 2-3's checkout-flow island). FOLLOW-UPS (out of scope): a Cancelled/refused lane isn't shown on the board (refuse lives in 3-5); payment-proof verify badges on the chip arrive with 3-5's order detail.
- (24 Jun · task 3-5) Operator order detail + verify/refuse + broadcast + end-of-day (US-035/036/037/038, FR-O-12/13/14/15). Created 3 pages + 2 islands. `src/app/(operator)/operator/orders/[id]/page.tsx` (Server, `force-dynamic`) — `getOrder(id)` + `listMenu()` (one read, no N+1) → bilingual line names, totals (MYR), payment method/status, and the DuitNow `proof_url` image (bg-contain, no remote-image config); `notFound()` on missing; mounts `payment-actions.tsx`. `src/components/operator/payment-actions.tsx` (`'use client'`) — Verify/Reject → `verifyPayment` (now notifies the customer of the verdict, see domain change) and a Cancel/Refuse control with a **required reason** → `refuseOrder` (only New/Confirmed; `data-refusable`); on success `router.refresh()`. `src/app/(operator)/operator/broadcast/page.tsx` + `src/components/operator/broadcast-form.tsx` — EN+AR textareas (AR `dir=rtl`), mirrors `broadcastSchema`, submits to `broadcast` (fans out once per opted-in customer via `dispatchBroadcast`), shows the send count or the `rate_limited` cap message. `src/app/(operator)/operator/end-of-day/page.tsx` (Server) — `endOfDay()` → completed orders / items sold / MYR revenue (delivered-only), with an empty state. DOMAIN CHANGE (MODIFY, allowed by brief): `src/lib/domain/orders.ts` — `verifyPayment` now calls a new `firePaymentEvent(id, decision)` (EVT-11 `payment_status`, service-role) so a rejection (and a verification) notifies the customer — the US-035 acceptance criterion the prior stub missed. Restock on refuse is handled by the existing DB trigger `restock_after_cancel` (status→cancelled returns reserved units + reverts sold_out→open), so the domain just sets status=cancelled. Added 23 i18n keys to en+ar (order_detail/reject/payment_proof/refuse_order/refuse/refuse_hint/not_refusable/verdict_saved/order_refused/message_en/message_ar/send_broadcast/broadcast_sent/broadcast_limit/broadcast_hint/completed_orders/items_sold/revenue/eod_hint/eod_empty); reused verify/reason/items/order_total/order_no/payment/cod/duitnow/pay_*/end_of_day/broadcast/error_generic. Test `tests/unit/operator-orders.test.ts` (13) — verifyPayment verdicts (update + one `payment_status` notification), refuseOrder New/Confirmed (status=cancelled+reason+notify) vs preparing (`conflict`, no write/notify), broadcast under-cap (fan-out + count) / `rate_limited` / empty, endOfDay delivered-only totals, and PaymentActions + BroadcastForm render (verify/reject + refuse gating, not-refusable note, ≥44px, bilingual). typecheck + lint clean; full test:unit green (14 files / 119 tests). NOTE: `payment_status` notification dedupe is per (order_id, event) — a re-verdict on the same order won't re-notify; acceptable for the one-review path. **Day 3 (operator app) COMPLETE.** FOLLOW-UPS (out of scope): the order chip on the board (3-4) doesn't yet deep-link to this detail page — wire the link in a later polish; broadcast has no preview/confirm step.
- (24 Jun · QA sweep — end of Day 3) Autonomous QA & consistency pass over the operator experience.
  **GREEN:** `npm run typecheck` · `npm run lint` (no warnings) · `npm run test:unit` (14 files / 119 tests) all pass. Day-3 operator app is complete vs the plan: Dashboard (one-tap Open/Close/Sold-Out) · Daily setup (qty/cutoff/window/zones) · Menu manager (CRUD/price/availability/photo) · Live order board (realtime advance/dispatch, legal transitions only) · Order detail (verify/reject proof, cancel/refuse w/ reason + restock via DB trigger) · Broadcast (bilingual fan-out + daily throttle) · End-of-day summary. Bilingual EN/AR at full parity (i18n-parity test green — identical key sets, AR≠EN, {{var}} token parity); 4 UI states per screen (layout OfflineBanner + group loading.tsx + empty/error in pages); tap targets ≥44px; RTL via logical props + `dir` on the AR text inputs/textareas; inventory never decremented/incremented client-side (place_order RPC + restock trigger own it).
  **FIXED (one file per commit):** `src/components/operator/order-chip.tsx` — the board order chip's order_no is now a `Link` to `/operator/orders/[id]`, closing the board→detail navigation gap flagged in the 3-4/3-5 notes (operator can tap any order on the board to verify payment / refuse). Authored as Alsakkaf-dev.
  **MISSING (for a later run — out of QA scope):** (a) broadcast has no preview/confirm step before fan-out; (b) the board has no Cancelled lane (refused orders just drop off); (c) the inline `lang==='ar'?…:…` customer-screen strings still aren't on `messages/*` (long-standing 1-4 follow-up, slated for 5-3/consolidation). All cosmetic/none block Day-4.
- (25 Jun · task 4-1) Notifications end-to-end correctness (US-022/038, FR-C-13/S-10/S-15). Audited every order-status write path: `createOrder`→order_received, `advanceOrder`→`fireStatusEvent`, `cancelOrder`/`refuseOrder`→cancelled, `riderPickup`/`riderDeliver`→`advanceOrder`, and `verifyPayment`→`firePaymentEvent` (added in 3-5) — every transition routes through `dispatchOrderEvent` exactly once. Confirmed dedupe is already enforced by the existing partial unique index `uq_notif_order_event on notifications(order_id, event) where order_id is not null` in 0001_init.sql (no new constraint needed) — `dispatchOrderEvent` inserts first and returns `{deduped:true}` on the 23505 violation, never re-sending. Confirmed fallback order: WhatsApp (`sendWhatsApp`) → on `{ok:false}` (incl. `not_configured`) Web Push for every `push_subscriptions` row → persist final channel/status. TIGHTENED the webhook receipt mapping: the old `.eq('status','sent').limit(1)` touched an arbitrary row — now we store the WhatsApp message id and map receipts precisely. Added migration `supabase/migrations/0004_notifications_provider_id.sql` (idempotent `add column if not exists provider_message_id text` + partial index) + mirrored it in `supabase/setup.sql` + the `Notification` type; `dispatch.ts` now writes `provider_message_id: wa.id` on a successful WA send; `webhooks/whatsapp/route.ts` POST maps each receipt by `provider_message_id = s.id` (skips when no id rather than touching an unrelated row); GET handshake unchanged. Test `tests/unit/notifications.test.ts` (7) + fixture `tests/fixtures/whatsapp-receipt.json` — dispatch sends WA once + records the message id, dedupe (2nd call no-op, no re-send), WA-fail→push fallback, broadcast fan-out one-per-opted-in-user in their language, `verifyWebhookSignature` valid/tampered/null, GET challenge echo vs 403, POST signed-receipt maps by message id (status=delivered) vs 401 on bad signature. typecheck + lint clean; full test:unit green (15 files / 126 tests). NO SECRETS inlined (WHATSAPP_*/VAPID_* via env only). FOLLOW-UP (out of scope): `web_push` receipts have no delivery webhook (only WA receipts update status) — acceptable, push has no delivery callback.
- (25 Jun · task 4-2) Realtime correctness across all 5 channels (US-010/021/032/044, NFR-P-01/R-04). Refactored `src/lib/realtime/hooks.ts` (signatures unchanged): (a) extracted pure, unit-tested merge helpers — `mergeOrder(prev,row,eventType)` now **dedupes INSERT by id** (a re-delivered INSERT updates in place instead of duplicating; fixes the board double-row risk), `mergeRiderFeed(prev,row)` (keep only ready/out_for_delivery, move to front, drop on delivered/cancelled), and `isDroppedStatus(status)`; (b) added a shared `makePoll` reconnect/poll fallback — each hook now subscribes with a status callback and, on `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`, polls its snapshot API every 10s (`/api/status`, `/api/board`, `/api/orders/[id]`, `/api/rider/deliveries`, all `cache:'no-store'`), stopping once `SUBSCRIBED` returns (degrades to last-known on fetch failure, never throws). Late-joiner correctness preserved: every hook seeds `useState(initial)` from the server snapshot. `useShopStatus` reflects both status + qty_remaining from the single `daily_session` UPDATE. Tests: `tests/unit/realtime-merge.test.ts` (11) — INSERT dedupe/prepend, UPDATE replace-one, rider-feed add/keep/remove, dropped-status detection; `tests/e2e/realtime.spec.ts` — asserts the seeded status region renders (late-joiner snapshot), self-skips offline (runs seeded in 5-2). typecheck + lint clean; full test:unit green (16 files / 134 tests). No regression to existing consumers (board/tracker/rider feed) — same return values. FOLLOW-UP (out of scope): poll interval is fixed 10s (no backoff); fine for the single-seller scale.
- (25 Jun · task 4-3) Rider flow completion & polish (US-039..044, FR-R-02..07). Audit: SCR-R-01 list (`rider/page.tsx`) already does today-only ready/out_for_delivery grouped by zone via `riderDeliveries()`, with empty/error/offline states, `RiderFeedSeed`+`useRiderFeed` live banner, and `DeliveryCard` (map link, tel link, items, total) — meets the bar, no change. `RiderActions` already h-16 (64px), disabled-while-busy, Picked-up while `ready` / Delivered while `out_for_delivery` → `riderPickup`/`riderDeliver` (which route through `advanceOrder` so the 4-1 notification fires) — meets the bar. FIX (US-040 gap): `rider/[id]/page.tsx` now shows a **payment method + status** row (`cod`/`duitnow` · `pay_*`, `data-payment` attr) — a rider must know if cash is due on COD; the detail previously showed items/total but not payment. Map deep-link confirmed: `buildMapsLink` produces a Google Maps `search` URL from pin (lat,lng) else URL-encoded address, opened in a new tab with `rel=noopener noreferrer` (FR-R-04). No new i18n keys (reused payment/cod/duitnow/pay_*). Tests: `tests/unit/rider-maps.test.ts` (4) — pin vs address fallback, empty, and 0,0-is-valid-pin edge; `tests/e2e/rider.spec.ts` — list→detail→assert map link + payment row + advance button, self-skips without a rider session (runs seeded in 5-2). typecheck + lint clean; full test:unit green (17 files / 138 tests). FOLLOW-UP (out of scope): the rider screens still use their own `<main>`/`BottomNav`/`LangToggle` (built before the route-group layout pattern) rather than an `(rider)` group layout — harmless, a later consolidation could hoist them like the customer/operator groups.
- (25 Jun · task 4-4) Automation & inventory integrity (US-045..049, FR-S-01..05). Verified the SQL guarantees in 0003_functions.sql: `place_order`'s atomic conditional `UPDATE daily_session SET qty_remaining = qty_remaining - k WHERE status='open' AND qty_remaining >= k` (raises `sold_out_or_insufficient` when not found — no global lock), auto sold-out when `v_remaining = 0` in the same txn, and `restock_after_cancel` (`least(qty_total, qty_remaining + old.item_count)` + `sold_out→open`) firing via the `orders_restock` trigger on status→cancelled. WIRED the scheduled auto-close on the free tier: created `src/app/api/admin/auto-close/route.ts` (POST, guarded by the `AUTO_CLOSE_SECRET` header, calls `auto_close_expired_sessions()` via the admin client) + `.github/workflows/auto-close.yml` (cron `*/5 5-11 * * *` UTC = 13:00–19:59 MYT, curls the route with the secret, no-ops if secrets unset) + added `AUTO_CLOSE_SECRET` to `.env.example`. MODIFIED `src/lib/utils/time.ts` — `withinTradingHours`/`pastCutoff` now take an optional injected `now` (defaults to `nowMyt()`, fully backward-compatible) so the MYT boundaries are deterministically testable without reimplementing TZ math. Tests: `tests/unit/inventory.test.ts` (8) — createOrder surfaces the RPC outcome (accepted / sold-out flag / concurrent-loser `sold_out_or_insufficient` with no notification), plus pure invariant model (decrement by k, sold-out at zero, never below zero, exactly-one-of-two concurrent last-portion, restock capped at total); `tests/unit/auto-close.test.ts` (5) — trading-window + cut-off boundaries, and the route runs the RPC only with the matching secret (401 otherwise). typecheck + lint clean; full test:unit green (19 files / 151 tests). NO SECRETS inlined (AUTO_CLOSE_SECRET via env + GitHub Actions secrets APP_URL/AUTO_CLOSE_SECRET). NOTE: true DB-level concurrency is enforced by the atomic SQL UPDATE (can't be exercised in a node unit test); the test models the contract and verifies the domain surfaces it. OWNER SETUP: set repo Actions secrets `APP_URL` (deployed URL) + `AUTO_CLOSE_SECRET` (matching the Vercel env var).
- (25 Jun · task 4-5) PWA shell / offline / install / push (EP-13, NFR-C-04, FR-S-11). Replaced the root layout's inline SW-registration script with proper client components: `src/components/pwa/register-sw.tsx` (registers `/sw.js` on load, no-ops where unsupported), `src/components/pwa/install-prompt.tsx` (captures `beforeinstallprompt`, dismissible bilingual A2HS banner, ≥44px), `src/components/pwa/push-optin.tsx` (tap → `Notification.requestPermission` → `pushManager.subscribe` with the VAPID key → POST to the existing `/api/push/subscribe`; exports the pure `urlBase64ToUint8Array`; hidden once granted/unsupported). Mounted all three in `src/app/layout.tsx` (manifest already linked via `metadata.manifest`; themeColor already set). Extended `public/sw.js` (cache `fahman-shell-v2`): precache `/`,`/menu`,`/offline`,`/manifest.webmanifest`; navigations network-first → cached shell → `/offline`; **stale-while-revalidate** for public `/api/status` + `/api/menu*` so the menu/status shell renders offline; never caches non-GET (authed mutations) or user-specific reads (board/orders); push + notificationclick handlers kept. Added 4 i18n keys to en+ar (install_app/install_hint/enable_notifications/dismiss). Tests: `tests/unit/pwa-vapid.test.ts` (3) — base64url→Uint8Array (padding, url-safe alphabet, 65-byte key); `tests/e2e/pwa.spec.ts` — manifest link present + SW registers, self-skips offline. typecheck + lint clean; full test:unit green (20 files / 154 tests). NO VAPID secret inlined (env only). **Day 4 (realtime/notifications/rider/automation/PWA) COMPLETE.** OWNER SETUP: replace the placeholder `public/icons/` (only README.txt today) with real 192/512/maskable PNGs so install shows the app icon; set `NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` for live push.
- (25 Jun · QA sweep — end of Day 4) Autonomous QA & consistency pass over realtime/notifications/rider/automation/PWA.
  **GREEN:** `npm run typecheck` · `npm run lint` (no warnings) · `npm run test:unit` (20 files / 154 tests) all pass. Day-4 vs plan is complete: notifications fire exactly once per transition with WA→push fallback + precise receipt mapping (4-1); all 5 realtime channels seed-from-snapshot, dedupe INSERT by id, and fall back to polling on socket drop (4-2); rider detail now shows payment method/status + map deep-link + one-tap pickup/deliver (4-3); the atomic decrement / sold-out-at-zero / restock-on-cancel guarantees are verified and the free-tier auto-close cron is wired behind a secret route (4-4); the PWA registers a SW, caches the menu/status shell offline, prompts install, and subscribes Web Push via VAPID (4-5). i18n parity green (the i18n-parity test passes with all new operator/board/detail/broadcast/eod/pwa keys); bilingual + RTL intact; ≥44px tap targets.
  **FIXED:** none needed — every Day-4 task was committed green; no cross-task inconsistency found (each task verified before commit, single-session sequential build so no concurrent-tree drift).
  **MISSING / DEFERRED (owner or Day-5):** (a) real PWA icons (placeholder dir today); (b) live secrets — Supabase keys, WhatsApp token, VAPID keys, AUTO_CLOSE_SECRET + GitHub Actions APP_URL/AUTO_CLOSE_SECRET (all env, none committed); (c) `npm run build` production-compile verification deferred to 5-4 (deploy-prep); (d) the long-standing inline-AR customer strings → `messages/*` consolidation, slated for 5-3. None block Day-5.
- (26 Jun · task 5-1) Vitest unit suite for pure/near-pure logic. Added 5 focused suites over existing modules (no app code changed): `tests/unit/orders.test.ts` (state machine — every legal edge true, skips/back-steps/late-cancels false, terminal states have no exits), `tests/unit/money.test.ts` (`sumLines` mixed + empty; `formatMYR` 2-decimal en + non-empty ar + en default), `tests/unit/time.test.ts` (MYT 13:00–19:00 boundaries via `vi.setSystemTime` — 14:00 open, 12:30/19:00/20:00 closed; `pastCutoff` HH:MM compare + null), `tests/unit/schemas.test.ts` (`createOrderSchema` empty-cart + delivery-refine + valid pickup/delivery; `otpVerifySchema` 6-digit; `configureSessionSchema` bad cutoff/neg qty), `tests/unit/api.test.ts` (`ok`/`fail` envelope + `httpStatus`/`ERROR_STATUS` code→status incl. unknown→500). typecheck + lint clean; full test:unit green (25 files / 176 tests).
- (26 Jun · task 5-2) Playwright E2E specs for the three role journeys + guards. Added `tests/e2e/customer.spec.ts` (browse menu → add → cart/checkout CTA; empty-cart state), `tests/e2e/operator.spec.ts` (dashboard shop controls; board renders columns-or-empty), `tests/e2e/guards.spec.ts` (RTL flip via NEXT_LOCALE cookie on /login; ≥44px tap-target via boundingBox on the login CTA — both run without a backend; sold-out + past-cut-off self-skip pending a seeded session). `tests/e2e/rider.spec.ts` (4-3) already covers the rider list→detail→advance journey; `tests/e2e/{order-board,realtime,pwa,i18n-switch}.spec.ts` add live-insertion, snapshot, PWA, and locale-persistence coverage. All specs assert real i18n strings (imported from messages/*), not hard-coded English, and self-skip gracefully when the app server / seeded backend isn't reachable so `npm run test:e2e` is green in CI (RTL + tap-target pass with the webServer up; data-dependent paths skip) and fully exercised against a seeded Supabase stack. typecheck + lint clean; unit suite unaffected (25 files / 176 tests). NOTE: `npm run test:e2e` needs `npx playwright install` + a built app (`npm run start`); the CI workflow (5-4) provisions both. Author-env here has no browsers/backend, so the gate verified is typecheck+lint+unit; e2e is authored + CI-wired.
- (26 Jun · task 5-3) Security + a11y + PDPA hardening. **Security:** bumped `next` 14.2.15→**14.2.35** and `eslint-config-next` to ~14.2.33 (resolved 14.2.35, latest patched 14.2.x) + `postcss`→^8.5.10; `npm run build` + typecheck green on the new version. `npm audit`: the postcss XSS advisory is fixed; the residual **next** advisories only fully resolve in Next 15/16 (a major upgrade — explicitly out of scope for a "patch 14.2.x" task; 14.2.35 carries every 14.2-line patch, and most residuals are self-hosted image-optimizer / SSR-cache DoS largely mitigated on Vercel + our all-dynamic routes; the CSP-nonce XSS doesn't apply — we set no nonces); the **esbuild/vite/vitest** and **glob/eslint-config-next** advisories are dev-tooling only (never shipped) and need breaking majors (vitest@4, eslint-config-next@16) — all JUSTIFIED & deferred (documented for the owner). Added a **Content-Security-Policy** to `next.config.mjs` (self + Supabase REST/wss + Storage images; `frame-ancestors 'none'`; `'unsafe-inline'` for Next's hydration) alongside the existing X-Content-Type-Options/X-Frame-Options/Referrer-Policy/Permissions-Policy. Verified no secrets inlined (all via env). **A11y:** confirmed `.btn`/`.field`/`.chip` carry `min-h-tap` (44px) in globals.css (tailwind `minHeight.tap=44px`); `aria-live` on the live status/timeline islands; `dir` flips with locale in `src/app/layout.tsx`; no `console.*` PII logging anywhere in `src`. **PDPA consent (US-007):** `src/app/login/page.tsx` consent now **defaults OFF** (was `true`) so the Send-code CTA stays disabled until the user opts in; consent copy moved out of JSX into `messages/*` (`consent_agree`, shown bilingually). Sign-in now stamps the policy version: new `src/lib/utils/consent.ts` `CONSENT_VERSION='1.0'`, written alongside `consent_at` in both `auth/otp/verify` + `auth/email/verify` upserts; migration `0005_consent_retention.sql` adds `users.consent_version`. **PDPA erasure (US-057):** `src/lib/domain/privacy.ts` (`requestErasure` self-service + `eraseUserById` operator-handled) deletes the user's addresses, scrubs phone→`deleted-<id>`/name→null, and writes an `erasure_audit` row (new table, RLS on, service-role only); exposed via `src/app/api/admin/erase/route.ts`. Test `tests/unit/privacy.test.ts` (6) — erasure deletes/scrubs/audits, empty-id rejected, CONSENT_VERSION present, and the login gate defaults consent off + disables submit. typecheck + lint + **build** green; full test:unit green (26 files / 181 tests). OWNER SETUP: run migration 0005 (or re-run setup.sql); the Next 15/16 residual advisories close on a future major upgrade (tracked).
- (26 Jun · task 5-4) Deploy-prep — env / CI / config / README. Verified `.env.example` is complete (every var the code reads: Supabase URL/anon/service-role/DB URL; WhatsApp token/phone-id/verify/app-secret; NEXT_PUBLIC_VAPID_PUBLIC_KEY + VAPID private/subject; NEXT_PUBLIC_DEFAULT_LOCALE/APP_URL; OTP + broadcast rate limits; AUTO_CLOSE_SECRET; TZ — each commented + NEXT_PUBLIC marked). Confirmed `.github/workflows/ci.yml` is green-by-design: `quality` (npm ci → lint → typecheck → unit → build with placeholder NEXT_PUBLIC envs) + `e2e` (npm ci → playwright install chromium → build → test:e2e); npm-cache enabled, runs on push/PR to main — left as-is (no change needed). Created **`BUILD/GO_LIVE_CHECKLIST.md`** — the owner's actionable list: Supabase project (Singapore) + migrate/seed + storage buckets + auth + operator/rider roles; WhatsApp Cloud API; VAPID + real icons; Vercel env + region; auto-close secret + Actions secrets; in-app trading config; a 3-role smoke test; and known limitations. Finalized **README.md**: corrected the stack table to the as-built versions (Next 14.2.x, React 18.3.x, npm — was Next 15/React 19/pnpm), fixed the env table (`VAPID_PUBLIC_KEY`→`NEXT_PUBLIC_VAPID_PUBLIC_KEY`, added APP_URL/OTP+broadcast limits/AUTO_CLOSE_SECRET/TZ rows), corrected the folder map (`lib/actions`→`lib/domain` + `lib/utils`), added a **DB migrate+seed order** subsection (0001→0005 / setup.sql) and linked the GO-LIVE checklist + auto-close note from Deployment. Migrations verified in order; the combined `supabase/setup.sql` carries the 0004/0005 additions for one-shot setup. typecheck + lint + unit green (181 tests); production `build` green (verified in 5-3). No secrets committed.
